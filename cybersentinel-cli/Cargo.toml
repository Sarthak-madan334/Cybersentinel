[package]
name = "cybersentinel-cli"
version = "0.1.0"
edition = "2021"
description = "Deterministic local security scanner for CyberSentinel"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
colored = "2.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
