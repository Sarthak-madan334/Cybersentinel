# AI usage note

Example submission wording: AI assistance was used to scaffold the Rust project structure, draft parsing helpers, and format the terminal report. The participant selected the suspicious-port list, SSH failure thresholds, process indicators, severity mapping, and incident JSON field mapping. The participant reviewed command-failure handling and verified the tool on a Linux host before submission.
